const legenda = document.createElement("div");
legenda.className = "klass1";
legenda.textContent = "Поднимите руки вверх! Это 1 Kla$$";

const insertLegend = document.querySelector(".oldSchool");

insertLegend.append(legenda)

function recognition1(recogn)
{
    const recognition = document.createElement("div");
    recognition.className = "recogn";
    recognition.textContent = recogn;

    const insertPhrase = document.querySelector(".rec1");
    insertPhrase.append(recognition);
    
}
function recognition2(recogn)
{
    const recognition = document.createElement("div");
    recognition.className = "recogn";
    recognition.textContent = recogn;

    const insertPhrase = document.querySelector(".rec2");
    insertPhrase.append(recognition);

}
function recognition3(recogn)
{
    const recognition = document.createElement("div");
    recognition.className = "recogn";
    recognition.textContent = recogn;

    const insertPhrase = document.querySelector(".rec3");
    insertPhrase.append(recognition);

}
function recognition4(recogn)
{
    const recognition = document.createElement("div");
    recognition.className = "recogn";
    recognition.textContent = recogn;

    const insertPhrase = document.querySelector(".rec4");
    insertPhrase.append(recognition);

}
function recognition5(recogn)
{
    const recognition = document.createElement("div");
    recognition.className = "recogn";
    recognition.textContent = recogn;

    const insertPhrase = document.querySelector(".rec5");
    insertPhrase.append(recognition);

}



// insertAdjacentHTML
// insertAdjacentText
// insertAdjacentElement



